server:
  port: 8090

spring:
  datasource:
    username: root
    password: abcd
    url: jdbc:mysql://localhost:3306/assignments?createDatabaseIfNotExist=true

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL5Dialect